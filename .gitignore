# ── Xcode / XcodeGen 生成物（源码用 project.yml，CI 里 xcodegen generate）──
ios-app/build/
ios-app/*.xcodeproj
ios-app/*.xcworkspace
DerivedData/
*.ipa
*.dSYM.zip
*.dSYM

# ── 本地产物（非源码）──
.DS_Store
# llama.cpp-omni 推理框架（由 scripts/build_xcframework.sh 生成，CI/本地构建，不入库）
ios-app/Frameworks/
deliverables/
.workbuddy/
.worktrees/

# ── 通用 ──
*.log
.env
